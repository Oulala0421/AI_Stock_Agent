# Streamlit Secrets Configuration Example
# 
# This file shows how to configure secrets for local development.
# Copy this file to .streamlit/secrets.toml and fill in your values.
#
# IMPORTANT: .streamlit/secrets.toml is in .gitignore
# Never commit your actual secrets to version control!

# MongoDB Connection
# Get your connection string from MongoDB Atlas
MONGODB_URI = "mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority"

# Alternative: Use environment variables instead
# The app will prioritize os.getenv() over st.secrets
# This makes it compatible with:
# - GitHub Codespaces (set in .devcontainer or repo secrets)
# - Render / Railway (set in environment variables)
# - Streamlit Cloud (use Streamlit's secrets management)
